import { describe, it } from "vitest";

/** T097 [US8] — Skip-marked: requires running server. */
describe.skip("agent products contract", () => {
  it("list and detail validate against product.schema.json", () => {});
});
