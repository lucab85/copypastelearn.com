import { describe, it } from "vitest";

/** T105 [US9] — UCP discovery contract. Skip-marked: requires running server. */
describe.skip("ucp discovery contract", () => {
  it("validates against ucp-discovery.schema.json", () => {});
});
